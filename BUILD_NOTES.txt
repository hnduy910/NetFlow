NetFlow Universal v4.1.11 Build 27 Ready

Required:
1. Xcode 15 or newer.
2. Select an Apple Development Team.
3. Use a unique bundle identifier if needed.
4. Build for iOS/iPadOS 16.0 or later.

Included fixes:
- Assets.xcassets and AppIcon are part of the Resources phase.
- Darwin import added for getifaddrs/if_data definitions.
- Local notification authorization and usage alerts implemented.
- Sampling pauses in background and refreshes when app becomes active.
- Info.plist includes encryption declaration and AppIcon name.
- project.yml excludes preview images from the app bundle.

Optional capability:
- Access WiFi Information for SSID. Enable only if the signing profile supports it.

No external dependency is required.

APP ICON UPDATE (v3.2)
- Integrated NetFlow / hnd icon into Assets.xcassets/AppIcon.appiconset.
- Source icon is a 1024x1024 opaque PNG with a white square background.
- No pre-rounded corners or transparency; iOS applies the correct device mask automatically.
- ASSETCATALOG_COMPILER_APPICON_NAME remains set to AppIcon for Debug and Release.

v3.6:
- Add NetFlow/NetFlow.entitlements with Access WiFi Information.
- VPN status now requires an active tunnel plus scoped system-network confirmation.
- Public IPv4 and IPv6 are fetched separately.
- Weather/place use current Core Location only.
- Carrier name uses CoreTelephony when iOS exposes it.

v4.1 audit:
- iOS 16-compatible toolbar placements.
- XcodeGen entitlement/config synchronization.
- Swift 5 minimal strict concurrency setting for Xcode 16.


PERSONAL TEAM FIX:
- Removed com.apple.developer.networking.wifi-info entitlement.
- Do not add Access Wi-Fi Information capability when signing with a Personal Team.
- SSID may be unavailable; all other core features remain buildable.

V4.1 NETWORK READER FIX:
- Rewritten NetworkInterfaceReader.swift.
- Removed restricted SSID APIs from Personal Team build.
- Fixed Darwin ifaddrs pointer handling and CoreLocation delegate isolation.

NETFLOW 4.1 CHANGES
- Replaced the circular usage chart with compact decimal Used / Remaining values.
- Usage values follow the selected plan unit (MB, GB, or TB).
- Improved VPN detection by requiring an active scoped tunnel, with NWPath .other fallback.
- Added VPN tunnel local IP when available.
- Data-plan capacity keyboard dismisses by tapping outside, scrolling, or tapping Done.
- Added last-updated time and unified refresh action on Overview.

V4.1: compact Day/Month/Year cycle picker; Last Updated moved to overview top.

V4.1.11 Build 27:
- Added explicit iPhone/iPad icon declarations and updated the green icon mark to hnd.
- Preserved the last non-VPN IPv4 while VPN is connected to avoid visible flicker.
- Refreshes non-VPN IPv4 only when Wi-Fi or Cellular network identity changes.
- Stores VPN public IP and country separately from the Wi-Fi/Cellular IPv4.
- Plan counters normalize immediately at app launch when a daily, monthly, yearly or custom cycle has ended.
- Samples crossing local midnight are split between the two dates so the new day does not inherit the previous day's traffic.
- Previous-cycle records remain available in History but are excluded from the active plan counter.
- Fixed persistence decoding so a user-selected daily, monthly, yearly or custom plan survives relaunch.
- Added ISO-8601 date decoding with a legacy date-format fallback.
- English localization audit completed, including Settings and language labels.
- About now displays marketing version, build number and build date on one line.
- Navigation titles now follow the selected app language.
- Location output is limited to commune/ward and province/city without street details or administrative prefixes.
